#include <iostream>
using namespace std;

int main(int argc, char const *argv[])
{
  // It would probably be a good idea to avoid recursively searching through a file. It appears that common practice is to use the .eof() flag
  cout << "Hello world!";
  return 0;
}
